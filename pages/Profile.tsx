import { ScrollView, View } from 'native-base'
import React from 'react'

const Profile = () => {
  return (
    <ScrollView>
        <View>
        
        </View>
    </ScrollView>
  )
}

export default Profile